# Files And Folders

入力欄にファイルまたはフォルダの絶対パスを入力して実行すると、その対象を開く。  
パスがフォルダの場合、末尾にパス区切り文字（`\`）を入力すると、そのフォルダ直下にある要素を候補として表示する。

![](../image/adhoc-command/filefolder/introduction.png)

パスを途中まで入力した状態で `Tab` キーを押すと、パスを補完する。  
フォルダを選択した状態で`Shift+Enter`キーを押下すると、そのフォルダ直下のファイルやフォルダをさらに表示する。


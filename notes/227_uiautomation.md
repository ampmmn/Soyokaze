# 227 UI要素関連

- タブコントロールの背面にある(非アクティブな)各タブのウインドウがどういう状態にあるかは対象アプリの実装次第
  - ウインドウを非表示にしているだけかもしれないし、タブ切替の都度、ウインドウを破棄・作成しているかもしれない
- なので、タブを切り替えずに、非アクティブなページ(上にあるUI要素)を取得する、というのは基本的にはできなさそう

- Migemize Windowsはタブコントロールに対して、すべてのタブのページ切替を行うことにより、各ページ上にあるUI要素を取得している
  - 最初この動作を見たとき、何だこれ、と思ったが、これは、それ以外におそらく方法がないから

- とはいえ、タブが次々切り替わる動作は癖が強く、同じ動作を自アプリに実装するのは少しためらわれる
  - 情報を取得する間、描画を抑制する/非表示にする/同じ絵を上に置く、などして切り替わる動作をみせないようにしたい
    - Copilotgチャットで質問したらWM_SETREDRAWで描画の抑制ができるという回答、本当?
    - あるいは取得する間、ShowWindow(SW_HIDE)で非表示にして終わったら元に戻すとか



